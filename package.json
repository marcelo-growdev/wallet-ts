{
  "name": "wallet-ts",
  "version": "1.0.0",
  "description": "## Iniciar um projeto TS",
  "scripts": {
    "build": "tsc && node ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
